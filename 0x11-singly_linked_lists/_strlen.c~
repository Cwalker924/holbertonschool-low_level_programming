#include "lists.h"

/**
 * _strlen - length of a string
 * @s: string passed in
 * Return: length of string
 */

int _strlen(const char *s)
{
	int i;

	i = 0;
	while (s[i] != '\0')
		i++;
	return (i);
}
